#include <stdio.h>

int main(void)
{
	int x = 10;
	int y = 10;
	int z = 33;

	x = x + 1; // x + =1
	y = y * 2; // y*=2
	z = z % (x + y); // z %= x + y

	printf("x = %d, y = %d, z = %d\n", x, y, z);

	bool AB = true;
	bool BC = false;

	return 0;
}
