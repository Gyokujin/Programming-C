#include <stdio.h>

int main()
{
	// 배열의 이름은 첫번째 값의 주소를 갖는다.

	int a[] = { 10,20,30,40,50 };

	printf("&a[0] = %d\n", &a[0]); // int의 메모리 크기는 4이기 때문에 4씩 주소값이 변경된다.
	printf("&a[1] = %d\n", &a[1]);
	printf("&a[2] = %d\n", &a[2]);
}
