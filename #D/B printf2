#include <stdio.h>

int main()
{
	printf("%2d %2d\n", 10, 10); // % d 사이의 숫자로 정렬이 가능하다.
	printf("%1d %2d %3d %4d %5d\n", 1, 2, 3, 4, 5); // % d 사이의 숫자로 정렬이 가능하다. 얼마나 공백을 만들지 정할수 있다
	printf("%-8d %-8d\n", 10, 10);
	printf("%+8d %+8d\n", 10, 10);
}
