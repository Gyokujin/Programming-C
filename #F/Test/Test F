#include <stdio.h>

// 1 ~ 100 사이의 정수 중에서 사용자가 입력한 수의 배수만 제외하고 모두 출력하라.

int main()
{
	int input = 0;
	printf("입력한 숫자를 제외하고 모두 출력하겠습니다. 1 ~ 100 사이의 숫자중 하나를 입력하세요. ");
	scanf_s("%d", &input);

	for (int i = 1; i <= 100; i++)
	{
		if (i % input != 0)
		{
			printf("%8d", i);
		}
	}
}
