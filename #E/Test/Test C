#include <stdio.h>

// 1에서 입력한 숫자 까지를 한줄에 5개씩 정렬하고 출력하는 코드를 작성하시오.

int main()
{
	int curNum = 0;
	scanf_s("%d", &curNum);

	for (int i = 1; i <= curNum; i++)
	{
		printf("%5d", i);

		if (i % 5 == 0)
			printf("\n");
	}
}
