#include <stdio.h>

int main()
{
    int total = 0;
    int num;

    printf("0부터 num까지의 덧셈, num은 ? : ");
    scanf_s("%d", &num);

    for (int i = 1; i <= num; i++) // for은 순번을 받아야 하는 경우에 주로 쓰인다
    {
        total += i;
    }

    printf("0~%d 까지의 총합은 : %d ", num, total);
}
