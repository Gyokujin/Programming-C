#include <stdio.h>
#define SIZE 5 // define으로 미리 배열의 크기를 정한다.

int main()
{
	int data[SIZE];

	for (int i = 0; i < SIZE; i++)
	{
		printf("정수를 입력하시오: ");
		scanf_s("%d", &data[i]);
	}

	for (int i = SIZE - 1; i >= 0; i--)
	{
		printf("%d\n", data[i]);
	}

	return 0;
}
