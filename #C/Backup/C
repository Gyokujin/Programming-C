#include <stdio.h>

int main(void)
{
    int x, y, z;
    char op;
    float zF;

    printf("수식을 입력해 주세요 \n (예:2+5)\n >>");

    scanf_s("%d %c %d", &x, &op, &y);

    if (op == '+')
    {
        z = x + y;
    }
    else if (op == '-')
    {
        z = x - y;
    }
    else if (op == '*')
    {
        z = x * y;
    }
    else if (op == '/')
    {
        zF = (float)x / y;
    }
    else if (op == '%')
    {
        z = x % y;
    }
    else
    {
        printf("지원하지 않는 연산자를 입력하셨습니다.\n");
    }

    if (op == '+' || op == '-' || op == '*' || op == '%')
    {
        printf("%d %c %d = %d", x, op, y, z);
    }
    else
    {
        printf("%d %c %d = %f", x, op, y, zF);
    }

}
