#include <stdio.h>

// 정수 두개를 입력하면 두 수의 합을 출력하는 함수를 작성하시오.

int Sum(int numA, int numB)
{
	int correct = numA + numB;
	return correct;
}

int main()
{
	int a = 0;
	int b = 0;
	scanf_s("%d %d", &a, &b);
	printf("%d + %d = %d", a, b, Sum(a, b));
}
