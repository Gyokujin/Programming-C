#include <stdio.h>

// 두 수 A와 B를 입력 받아서 크기 비교하기

void Comparison(int numA, int numB)
{
	printf("%d이(가) %d보다 큽니다.", numA > numB ? numA : numB, numA > numB ? numB : numA);
}

int main()
{
	int a, b;
	printf("크기를 비교할 두 수를 입력하세요. ");
	scanf_s("%d %d", &a, &b);
	Comparison(a, b);
}
