#include <stdio.h>

int main()
{
	int numA = 8;
	int numB = 4;
	int numC = 10;

	printf("%d\n", numA < numC && numB < numC); // &&(and) 전체 조건이 참일 경우 참을 반환
	printf("%d\n", numB > numC || numA < numC); // ||(or) 전체 조건중 하나라도 참이면 참을 반환
	printf("%d\n", !(numB > numC)); // !(not) 참은 거짓을 거짓은 참을 반환
}
