#include <stdio.h>

// 길이가 10인 배열을 선언하고, main에서 정수를 입력 받는다.
int main()
{
	int numbers[10];

	for (int i = 0; i < 10; i++)
	{
		scanf_s("%d", &numbers[i]);
	}

	for (int i = 0; i < 10; i++)
	{
		printf("%4d", numbers[i]);
	}
}
